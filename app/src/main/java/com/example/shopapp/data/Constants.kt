package com.example.shopapp.data

object Constants {

    object Database {
        const val NAME = "shop.db"
    }

    object Product {
        const val PAGE_LIMIT = 20
    }

    object Api {
        const val TIMEOUT = 20
    }
}